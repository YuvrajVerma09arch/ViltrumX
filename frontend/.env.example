# Copy to .env for local development.
# Points the SPA at the Django API. In containers this is injected by
# docker-compose; in production set it to the deployed API origin.
VITE_API_URL=http://localhost:8000/api/v1
