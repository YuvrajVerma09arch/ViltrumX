# ViltrumX backend — core runtime (installed by CI on every push).
# Heavy ML/agent deps live in requirements-ml.txt (Week 3, installed on demand).

# Web framework
Django>=5.1,<6
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4

# Config / DB
dj-database-url>=2.2
psycopg[binary]>=3.2

# Async jobs + realtime
celery[redis]>=5.4
redis>=5.0
channels>=4.1
channels-redis>=4.2
# ASGI server — required for WebSockets. `manage.py runserver` is WSGI-only
# and 404s the /ws/ upgrade, so the Command Deck stream needs daphne.
daphne>=4.1

# Graph (Week 2)
neo4j>=5.23

# Dev / CI quality gates
ruff>=0.6
bandit>=1.7
pytest>=8.3
pytest-django>=4.9
pytest-cov>=5.0
